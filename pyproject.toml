[project]
name = "gpulab"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "modal>=1.0",
    "torch",
    "triton",
    "numpy",
    "rich",
]

[project.scripts]
gpulab = "scripts.run:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
